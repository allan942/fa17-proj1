<h3>Trainer</h3>
<p>Name: <%= @trainer.name if @trainer.name %></p>
<p>Email: <%= @trainer.email if @trainer.email %></p>
<p>Pokemon: <% @trainer.pokemons.each do |p| %>
				<p><%= p.name %></p>
				<p>Level <%= p.level %></p>
				<p>Health <%= p.health %></p><br />
				<%= button_to "Damage!", damage_path(p), :class => "button medium", :method => :patch %>
			<% end %></p>
<p>Create New Pokemon!</p>
<%= button_to "Create!", new_path, :class => "button medium", :method => :get %>